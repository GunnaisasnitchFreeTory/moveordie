---
description: Ensure systems are safe, verifiable, and debuggable
alwaysApply: false
---

# Ship Safe

All systems must include basic safety checks.

Examples:

- validate inputs
- guard against nil values
- provide debug logs when necessary

Example:

if not player then
    warn("SpawnSystem: player missing")
    return
end

Systems should fail safely instead of crashing the game.
